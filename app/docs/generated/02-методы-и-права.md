<!-- СОБРАНО АВТОМАТИЧЕСКИ. Не редактировать руками: правки затрутся.
     Пересобрать: python app/docgen.py -->

# Методы кабинета и права

Собрано из `iigbot/api.py`: все методы с декоратором `@safe` доступны с фронта
как `POST /api/<имя>`. Колонка «Кому» выведена из вызовов проверок прав внутри метода.

| Метод | Аргументы | Кому | Что делает |
|---|---|---|---|
| `alert_link` | `—` | любой вошедший | Одноразовая deep-link для НАДЁЖНОЙ привязки лички к бюджет-алертам: пользователь |
| `alert_unlink` | `—` | любой вошедший | Отвязать личку от алертов. |
| `assign_client` | `login, user_id, delivery` | админ или наблюдатель | Назначить клиента специалисту (user_id=None/'' → общий пул) и сразу задать способ |
| `assignable_users` | `—` | админ или наблюдатель | Список специалистов/админов для раздачи проектов (наблюдателю и админу). |
| `audit_log` | `limit, action, user_id` | админ или наблюдатель | Журнал действий для работодателя/админа: привязки, назначения, долги, сотрудники. |
| `backup_cloud_now` | `—` | только админ | Увезти копию базы на Google-диск прямо сейчас (админ). |
| `backup_cloud_state` | `—` | только админ | Настроена ли выгрузка наружу и когда была последней (админ). |
| `backup_list` | `—` | только админ | Список бэкапов (админ): что есть, когда, сколько весит. |
| `backup_now` | `—` | только админ | Сделать бэкап прямо сейчас (админ). |
| `bind` | `chat_id, login` | запрещено наблюдателю |  |
| `bind_bulk` | `min_confidence, owner_id` | админ или наблюдатель (в части случаев); запрещено наблюдателю (в части случаев) | Массовая привязка: привязывает все непривязанные чаты, где уверенность подсказки |
| `bind_for` | `chat_id, login, owner_id, delivery` | админ или наблюдатель | Привязать чат к клиенту и СРАЗУ назначить владельца-специалиста (админ/наблюдатель). |
| `budgets` | `scope` | любой вошедший | Вкладка «Бюджеты». scope='all' — по всему агентству (админ/наблюдатель: у админа может |
| `budgets_refresh` | `—` | любой вошедший | Принудительный сбор бюджетов в фоне по МОИМ клиентам (специалист/админ — свои, |
| `chat_check` | `chat_id` | любой вошедший | Бот всё ещё в чате? Раньше это выяснялось в момент отправки клиенту. |
| `chats` | `—` | любой вошедший |  |
| `client` | `login` | только свой клиент |  |
| `client_campaigns` | `login` | только свой клиент | Кампании клиента: выбрать эталон для слепка или просто посмотреть, что есть. |
| `client_goals` | `login` | только свой клиент | Цели клиента для выбора в конструкторе. |
| `client_goals_pull` | `login` | запрещено наблюдателю; только свой клиент | Подтянуть цели из Метрики для ОДНОГО клиента и СОХРАНИТЬ (кнопка в конструкторе, когда |
| `client_sheet_get` | `login` | только свой клиент | Что сейчас привязано к клиенту — для формы. |
| `client_sheet_set` | `login, url` | запрещено наблюдателю; только свой клиент | Привязать Google-таблицу к клиенту ссылкой. Пустая строка — отвязать. |
| `clients` | `—` | любой вошедший |  |
| `cloud_pull` | `—` | только админ | Тянет привязки/цели из общей таблицы в локальную базу. |
| `cloud_push` | `—` | только админ | Заливает локальные привязки/цели в общую таблицу. |
| `cloud_status` | `—` | только админ | Состояние общей таблицы привязок (админ). |
| `connect_link` | `login` | любой вошедший |  |
| `copy_reports` | `logins` | любой вошедший | Собирает недельные отчёты пачкой для КОПИПАСТА в сторонние мессенджеры (WhatsApp/VK/MAX/ |
| `counter_check` | `login` | только свой клиент | Доступен ли счётчик Метрики у клиента — до того, как тянуть цели. |
| `counters_backfill` | `only_active` | админ или наблюдатель | Достать счётчики из кампаний Директа и запомнить (админ/наблюдатель). |
| `coverage_history` | `weeks` | админ или наблюдатель | Тренд сдачи по неделям: агентство и каждый специалист (для графиков в Контроле). |
| `dashboard` | `—` | любой вошедший | Сводка Обзора. Десяток обращений к базе на каждое открытие — а открывают |
| `delete_chat` | `chat_id` | только админ | Удаляет чат из базы (для «висяков» — когда бота уже выгнали, а строка осталась). |
| `dialog_dismiss` | `chat_id` | любой вошедший | «Ответ не нужен»: убрать чат из ожидающих (клиент написал «Отлично» — и всё). |
| `dialog_restore` | `chat_id` | любой вошедший | Вернуть чат в «Ждут ответа» (отмена «ответ не нужен»). Работодателю тоже можно. |
| `dialogs` | `—` | любой вошедший | Вкладка «Ждут ответа»: клиенты, которым мы не ответили, и забытые чаты. |
| `digest_preview` | `—` | админ или наблюдатель | Посмотреть текст сводки, не отправляя. |
| `digest_send` | `—` | админ или наблюдатель | Отправить сводку всем подписчикам «по всему агентству» (alert_scope='all'). |
| `docs_list` | `—` | любой вошедший | Оглавление раздела: человеческие документы и собранные из кода справочники. |
| `docs_read` | `doc_id` | только админ (в части случаев) | Текст одного документа. doc_id — только из docs_list, обходы пути запрещены. |
| `dossier` | `login, a_from, a_to, b_from, b_to, attribution, goal_ids, cuts, sheet_url` | только свой клиент | Досье: период A против периода B. Даты обоих периодов считает кабинет |
| `dossier_cut` | `login, cut, a_from, a_to, b_from, b_to, attribution, goal_ids` | только свой клиент | Один дополнительный разрез досье. Кабинет запрашивает их по очереди, |
| `dossier_options` | `—` | любой вошедший | Справочник разрезов для вкладки «Досье». |
| `error_log` | `lines` | только админ | Хвост файлового журнала iig_errors.log для раздела «Журнал»: новые сверху, |
| `error_log_clear` | `—` | только админ | Очищает журнал ошибок (админ). |
| `excuse_add` | `login, kind, reason` | любой вошедший | Закрыть «долг» по клиенту: уважительная причина, что отчёт не отправлен. |
| `excuse_add_bulk` | `logins, kind, reason` | любой вошедший | Закрыть сразу пачку долгов одной причиной (кнопка «закрыть все» / выбранные). |
| `excuse_remove` | `excuse_id` | любой вошедший | Вернуть долг (снять уважительную). Наблюдатель/админ, или владелец клиента. |
| `excuses_list` | `—` | админ или наблюдатель | Список уважительных (для наблюдателя/админа) — что и почему закрыто. |
| `exp_autotarget` | `days, threshold` | ограничено своими клиентами | У кого автотаргетинг съедает большую часть денег, притом что фразы в кампаниях есть. |
| `exp_bench` | `base, days` | ограничено своими клиентами | Свои проекты, померенные выбранной линейкой. |
| `exp_conv_trust` | `login, days` | только свой клиент | Сколько из конверсий — настоящие обращения, а сколько поведение на сайте. |
| `exp_idle` | `—` | ограничено своими клиентами | Кто встал: за 7 дней ноль, а за 21 день деньги были. |
| `exp_note` | `key, text` | любой вошедший |  |
| `exp_notes` | `key` | любой вошедший |  |
| `exp_overspend` | `threshold` | ограничено своими клиентами | Перерасход честно: факт сравнивается только с теми кампаниями, у которых лимит есть. |
| `exp_spike` | `days, times` | ограничено своими клиентами | День, когда потратили кратно больше обычного. |
| `exp_standard` | `—` | ограничено своими клиентами | Сверка активных кампаний со стандартом агентства. |
| `exp_status` | `key, status` | только админ |  |
| `exp_summary` | `—` | ограничено своими клиентами | Итоги: кто лучший и худший по каждому показателю, и что изменилось. |
| `exp_trash` | `login, days, min_clicks, min_cost` | только свой клиент | Кандидаты в запрет площадок по одному клиенту. |
| `exp_vote` | `key, param, score` | любой вошедший | Оценить можно только то, что сам запускал. |
| `experiments` | `—` | любой вошедший | Список фич на испытании со статистикой использования и моими оценками. |
| `gsheets_breakdowns` | `login, which, date_from, date_to` | запрещено наблюдателю; только свой клиент | Создаёт НОВЫЕ листы-снимки разрезов (По РК/группам/ключам/поисковым фразам/регионам) |
| `gsheets_clients` | `—` | любой вошедший | Клиенты (в моём скоупе) с Google-таблицей: сперва привязанные ссылкой из кабинета, |
| `gsheets_push` | `login` | запрещено наблюдателю; только свой клиент | Авто-заполняет листы-ленты Google-таблицы клиента свежими данными из Директа: |
| `gsheets_push_start` | `login` | запрещено наблюдателю; только свой клиент | Выгрузка в Google-таблицу клиента в фоне. |
| `gsheets_status` | `—` | любой вошедший | Доступен ли сервисный ключ и какие таблицы расшарены на сервисный аккаунт. |
| `health_last` | `—` | только админ | Последний известный результат — чтобы экран не был пустым до проверки. |
| `health_start` | `—` | только админ | Проверка всех интеграций в фоне: каждый вызов — секунды. |
| `history` | `—` | ограничено своими клиентами |  |
| `import_chat_history` | `payload` | запрещено наблюдателю | Импорт истории чата из экспорта Telegram Desktop (result.json). |
| `import_config` | `—` | только админ |  |
| `job_progress` | `key` | любой вошедший | Состояние фоновой задачи. Переживает перезапуск приложения: лежит в базе. |
| `jobs_state` | `—` | любой вошедший | Что идёт прямо сейчас и что было недавно — для строки в шапке. |
| `metrika_goals` | `login` | только свой клиент | Для карточки клиента: цели из Метрики с пресетом active (ключевые отмечены). Не сохраняет. |
| `metrika_goals_bulk` | `—` | запрещено наблюдателю | Подтягивает цели из Метрики для всех ПРИВЯЗАННЫХ клиентов и СОХРАНЯЕТ их (с пресетом |
| `metrika_goals_bulk_start` | `—` | запрещено наблюдателю | «Цели из Метрики (всем)» в фоне: ходит в Метрику по каждому клиенту. |
| `my_alert` | `—` | любой вошедший | Куда мне идут бюджет-алерты: linked=True — привязана личка по deep-link (надёжно, работает |
| `my_note` | `—` | любой вошедший | Своя приписка пользователя: note=null — общая (из Настроек), '' — без приписки, |
| `my_notes` | `—` | любой вошедший | Неподтверждённые сообщения текущему пользователю (яркий баннер). Любой вошедший. |
| `note_ack` | `note_id` | любой вошедший | «Прочитано» — убрать баннер у текущего пользователя. |
| `note_create` | `to_user, text, kind` | админ или наблюдатель | Оставить сообщение сотруднику (to_user=None/'all' → всем специалистам). Наблюдатель/админ. |
| `note_delete` | `note_id` | админ или наблюдатель |  |
| `note_reply` | `note_id, text` | любой вошедший | Специалист отвечает на сообщение работодателя. Ответ виден наблюдателю в Контроле. |
| `notes_list` | `—` | админ или наблюдатель | Отправленные сообщения с числом прочтений и ответами специалистов (наблюдатель/админ). |
| `notif_read` | `notif_id` | любой вошедший | Отметить одно уведомление прочитанным (или все, если id не задан). |
| `notifications` | `limit` | любой вошедший | Уведомления текущего пользователя + счётчик непрочитанных. |
| `pool_clients` | `—` | админ или наблюдатель | Все клиенты агентства с владельцем и способом доставки — для раздачи (админ/наблюдатель). |
| `preset_apply` | `preset_id, login, custom_name` | запрещено наблюдателю; только свой клиент | Создать кампанию по шаблону. Фоном: два обращения к Директу, а процесс один. |
| `preset_delete` | `preset_id` | запрещено наблюдателю |  |
| `preset_from_campaign` | `login, campaign_id` | только свой клиент | Снять шаблон с существующей кампании. Самый короткий путь: специалист один раз |
| `preset_get` | `preset_id` | любой вошедший | Шаблон целиком — для формы правки. |
| `preset_label_set` | `code, text` | только админ | Подписать настройку так, как она названа в интерфейсе Директа. |
| `preset_preview` | `preset_id, login, custom_name` | только свой клиент | Что именно появится на аккаунте. Показываем до записи — и только после |
| `preset_runs` | `limit` | ограничено своими клиентами | Что создавали по шаблонам: свои проекты — специалисту, всё — админу. |
| `preset_save` | `preset_id, name, note, data, shared` | запрещено наблюдателю | Создать или обновить шаблон. Проверяем до записи — чтобы кривой шаблон |
| `preset_spec` | `—` | любой вошедший | Справочники для формы шаблона: стратегии, настройки, корректировки, атрибуция. |
| `preset_undo` | `run_id` | запрещено наблюдателю; только свой клиент | Удалить кампанию, созданную по шаблону. Директ разрешает удалять только те, |
| `presets_list` | `—` | любой вошедший | Шаблоны, доступные текущему пользователю. |
| `preview` | `login` | только свой клиент |  |
| `reassign_all` | `from_user_id, to_user_id` | админ или наблюдатель | Перекинуть ВСЕ проекты одного специалиста другому (увольнение, отпуск, передача дел). |
| `report_campaigns` | `login` | только свой клиент | Список кампаний клиента для фильтра конструктора (только чтение). |
| `report_export_xlsx` | `login, level, date_from, date_to, attribution, limit, segments, date_grain, campaign, goal_ids` | любой вошедший | Строит отчёт и сохраняет .xlsx в подпапку reports/ рядом с программой. Ничего не отправляет. |
| `report_options` | `—` | любой вошедший |  |
| `report_query` | `login, level, date_from, date_to, attribution, limit, segments, date_grain, campaign, goal_ids` | любой вошедший |  |
| `report_send` | `login, level, date_from, date_to, attribution, limit, segments, date_grain, campaign, goal_ids` | запрещено наблюдателю |  |
| `resend_report` | `login` | запрещено наблюдателю; только свой клиент | Отправить недельный отчёт по клиенту заново (кнопка в Истории). |
| `run_weekly` | `—` | запрещено наблюдателю |  |
| `run_weekly_progress` | `—` | любой вошедший |  |
| `run_weekly_start` | `only_failed, dry_run` | запрещено наблюдателю | Запускает рассылку в фоне (для окна прогресса). only_failed=True — только тем, кто в |
| `save_client` | `login, name, goals, attribution, delivery` | только свой клиент; запрещено наблюдателю (в части случаев) |  |
| `save_secrets` | `telegram_bot_token, yandex_oauth_token` | только админ | Сохраняет токены в secrets.json прямо из интерфейса (без правки файла руками). |
| `save_settings` | `intro, specialist_note, attribution_model, admin_user_ids, report_day, report_time` | только админ | Настройки агентства целиком, включая список администраторов бота. Только админ. |
| `seed_dialogs_from_history` | `—` | запрещено наблюдателю | Проинициализировать «последний контакт» из истории наших отправок (без переписки). |
| `send_test` | `login` | запрещено наблюдателю; только свой клиент |  |
| `set_client_note` | `login, note` | любой вошедший | Заметка по проекту («клиент на паузе до августа») — видна всем причастным. |
| `set_delivery` | `login, mode` | запрещено наблюдателю; только свой клиент | Быстрый тумблер способа доставки клиента (из вкладки «Сторонние»): |
| `set_delivery_super` | `login, mode` | админ или наблюдатель | Сменить доставку клиента при раздаче (админ/наблюдатель), НЕ трогая владельца. |
| `set_my_alert` | `username` | любой вошедший | Задать свой @username для алертов (пусто = не получать персонально). Наблюдателю можно. |
| `set_my_alert_scope` | `scope` | любой вошедший | Охват алертов: 'all' — по всем клиентам агентства (админ/наблюдатель), иначе только свои. |
| `set_my_note` | `note` | запрещено наблюдателю | Сохранить свою приписку (null=общая, ''=без, текст=своя). Подставляется во все отчёты |
| `settings` | `—` | любой вошедший |  |
| `sheet_column_set` | `login, title, goal_ids, clear` | запрещено наблюдателю; только свой клиент | Задать разметку столбца. clear=True — вернуть угадывание по названию. |
| `sheet_columns` | `login, tab` | только свой клиент | Разметка столбцов ленты: что программа заполнит, а что не тронет. |
| `specialist_card` | `user_id` | админ или наблюдатель | Досье специалиста: проекты (покрытие/бюджет/доставка/заметка), тренд сдачи, |
| `suggestions` | `—` | любой вошедший |  |
| `supervision` | `—` | админ или наблюдатель | Контроль: по каждому сотруднику — покрытие недельной рассылки (все ли его привязанные |
| `sync_clients` | `—` | админ или наблюдатель | Подтянуть список клиентов агентства из Директа. Доступно админу И наблюдателю: |
| `sync_clients_start` | `—` | админ или наблюдатель | «Подтянуть из Директа» в фоне: 393 клиента — это десятки секунд. |
| `sys_backup_send` | `—` | только админ | Копия базы во внешнее хранилище: файлом в личный чат с ботом. |
| `sys_cpu` | `—` | только админ | Расход процессора: по дням и по методам. На виртуальном хостинге это тот ресурс, |
| `sys_deploys` | `—` | только админ | Копии файлов приложения, которые кладёт выкат: откуда можно откатиться. |
| `sys_integrity` | `—` | только админ | Полная проверка базы. Читает файл целиком — поэтому в фоне. |
| `sys_maintenance` | `—` | только админ | ANALYZE + VACUUM: пересчитать статистику и сжать файл базы. |
| `sys_ping` | `—` | только админ | Проверка снаружи: запрос уходит отдельным потоком. |
| `sys_restart` | `—` | только админ | Перезапустить приложение (Passenger следит за tmp/restart.txt). |
| `sys_restore` | `file` | только админ | Восстановить базу из копии. Текущее состояние сначала сохраняем отдельным бэкапом: |
| `sys_rollback` | `stamp` | только админ | Вернуть файлы приложения из выбранной копии и перезапустить приложение. |
| `sys_runbook` | `—` | только админ | Справка для преемника с подставленными путями этой машины. |
| `sys_secret_rotated` | `name` | только админ | Отметить, что ключ обновлён вручную: возраст считается от этой даты. |
| `sys_secrets` | `—` | только админ | Какие ключи есть, когда менялись, не пора ли обновить. Значения не отдаём. |
| `sys_status` | `—` | только админ | Одним запросом всё о машине: версия, аптайм, база, место, задачи, проверки. |
| `sys_watch` | `—` | только админ | Последний результат проверки живости — и ручной, и ночной из cron. |
| `unbind` | `chat_id` | запрещено наблюдателю |  |
| `user_create` | `email, password, name, role` | админ или наблюдатель; только админ (в части случаев) | Завести сотрудника. Наблюдатель (работодатель) тоже может — он же нанимает; но выдать |
| `user_set_active` | `user_id, active` | любой вошедший |  |
| `user_set_password` | `user_id, password` | любой вошедший |  |
| `user_set_role` | `user_id, role` | только админ (в части случаев) | Сменить роль (в т.ч. выдать «Наблюдатель»). Роль «Администратор» назначает только админ. |
| `users_list` | `—` | админ или наблюдатель | Список сотрудников (админ и наблюдатель — работодателю нужно управлять командой). |
| `workload` | `—` | админ или наблюдатель | Баланс нагрузки: сколько проектов у кого, сколько ничьих, кто перегружен. |

Всего методов: **151**.

## Роли

| Роль | Видит клиентов | Может менять |
|---|---|---|
| `admin` | только своих | да |
| `user` (специалист) | только своих | да |
| `observer` (работодатель) | всех | нет, режим просмотра |
| без пользователя (десктоп) | всех | да |
