# Версии закреплены точно, а не «не ниже».
#
# Было `requests>=2.31`: это значит, что завтра выходит новая мажорная версия,
# приезжает при первой же переустановке, и кабинет ломается без единой правки
# в нашем коде. Причину такого сбоя ищут долго, потому что искать её начинают
# в своём коде.
#
# Цифры ниже сняты с боевого окружения (Python 3.10.12 на Beget) 21.08.2026 —
# то есть это ровно то, на чём система работает прямо сейчас, а не то, что
# должно бы работать.
#
# Обновлять осознанно: поднять версию, прогнать `pytest app/tests`, выкатить.

requests==2.34.2
flask==3.1.3
openpyxl==3.1.5
gspread==6.2.1
google-auth==2.55.2
google-auth-oauthlib==1.4.0

# Тянутся как зависимости, но закреплены явно: именно они чаще всего ломают
# соединение с Директом и Google при самостоятельном обновлении.
urllib3==2.7.0
certifi==2026.6.17
cryptography==49.0.0

# Только для десктопной сборки, на сервере не нужен и не ставится.
pywebview==5.4 ; sys_platform == "win32"
