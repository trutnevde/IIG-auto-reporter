# HANDOFF — IIG Reporter (передача между сессиями/устройствами)

> Этот файл — «точка входа» для продолжения работы на другом устройстве или в новой сессии.
> Память Claude локальна (на конкретной машине) и НЕ переезжает — поэтому всё важное здесь.

## Как продолжить на другом устройстве
1. Склонировать/обновить репозиторий: `git clone https://github.com/trutnevde/IIG-auto-reporter` (или `git pull`).
2. Открыть сессию Claude Code в папке репозитория.
3. Сказать ассистенту: **«Прочитай HANDOFF.md и продолжай».**
4. Рядом с exe должен лежать `secrets.json` (токены) — он НЕ в репозитории. Без него можно править код, но не тестировать вживую.

Последний коммит на момент написания: **1c9057c** (ветка `main`).

## Что это за проект
IIG Reporter — десктоп/веб-приложение (PyInstaller onefile `IIGReporter.exe`), которое рассылает
еженедельные отчёты Яндекс.Директ → Telegram по клиентам агентства. Собирается на GitHub Actions,
exe лежит в релизе `latest`. Уже в боевом использовании (десятки клиентов).

## Текущее состояние (работает)
- Один exe = интерфейс + фоновый слушатель Telegram (отдельным процессом). `secrets.json` рядом с exe,
  токены можно вводить прямо в Настройках.
- Клиенты подтягиваются из Директа кнопкой; привязки чат↔клиент; еженедельная рассылка;
  удаление чатов-«висяков» (🗑 во вкладке «Чаты»); вкладка «Гайд».
- **Конструктор отчётов** (Отчёты → Конструктор): базовый разрез account/campaign/adgroup/ad/keyword/searchquery
  + **множественные срезы** (Дата с группировкой день/неделя/месяц, Устройство, Пол, Возраст, Гео, Сеть),
  9 моделей атрибуции (LSC/LC/FC/LYDC/LSCD/LCD/FCD/LYDCD/AUTO), свободные даты, **выбор колонок-метрик**,
  **конверсии/CR/CPA без целей** (поле Conversions), копировать/отправить в чат, **выгрузка xlsx** (папка `reports/` рядом с exe).
  Подписи значений срезов человекочитаемые (Десктоп/Смартфоны, Женщины/Мужчины, 25–34, РСЯ/Поиск — `report_custom._pretty`).
  В таблице результата: **сортировка** по клику на заголовок (▲▼) + **поиск по строкам**. **«Кампания (фильтр)»**
  (`yandex.get_campaigns` → `campaigns.get`; `api.report_campaigns`) ограничивает отчёт одной кампанией
  (фильтр `CampaignId` в `SelectionCriteria` через `report.fetch_report(filters=…)`) — разбивка внутри неё.

## Критичные факты окружения
- **Telegram (`api.telegram.org`) заблокирован у пользователя (РФ, DPI)** → нужен VPN в режиме «весь трафик»
  (full-tunnel). Яндекс работает напрямую. Симптом блокировки: статус «Нет связи», ошибки отправки.
- **База `iigbot.sqlite3` лежит рядом с exe и СВОЯ для каждой папки/пользователя Windows.** Привязки/цели
  не переезжают между аккаунтами Windows сами — копировать этот файл.
- **Один бот = один слушатель** (getUpdates отдаёт обновления только одному). Нельзя держать две запущенные
  копии одного бота одновременно — будут дубли/конфликты.

## Токены
- `secrets.json`: `telegram_bot_token` + `yandex_oauth_token`.
- **2026-06-23: `yandex_oauth_token` заменён на ОБЩИЙ токен (аккаунт `advert-iig`)** с правами `direct:api`
  + `metrika:read`. Проверено вживую: Директ 393 клиента, Метрика 268 счётчиков. Бэкап старого —
  `secrets.json.bak` рядом с exe. Покрытие Метрики неполное (268 из 393) — где доступа к счётчику нет,
  остаётся «всего конверсий».

## 🔑 КЛЮЧЕВАЯ НАХОДКА (2026-06-22) — ✅ РЕАЛИЗОВАНО (2026-06-23)
**Конверсии доступны БЕЗ целей и БЕЗ Метрики.** Reports API Директа отдаёт поле `Conversions` даже без
параметра `Goals` (проверено вживую: campaign-отчёт для `porg-qpbeanml` вернул колонку Conversions,
напр. 2808 у одной кампании). Реализовано в `report_custom.build`: если у клиента нет целей — конверсии
берутся из «голого» `Conversions`; если цели есть — суммируются goal-колонки под выбранной атрибуцией.

Также проверены и работают доп. измерения для «мастера как в Директе»:
`Device`, `Gender`, `Age`, `LocationOfPresenceName` (гео), `AdNetworkType`. Все вернули данные.

## Что делать дальше (приоритеты пользователя)
1. ✅ **СДЕЛАНО (2026-06-23)** — гибкий «Мастер отчётов»: конверсии/CR/CPA без целей; множественные срезы
   (Дата день/неделя/месяц, Устройство, Пол, Возраст, Гео, Сеть); выбор колонок-метрик; `report_custom.build`
   обобщён до произвольного набора FieldNames + агрегация на нашей стороне (для свёртки дат). **xlsx-выгрузка**
   тоже (openpyxl добавлен в requirements + spec). Покрыто офлайн-тестами (мок Reports API). НЕ тестировалось
   вживую — в чатах живой бот, отправки = спам.
2. ✅ **СДЕЛАНО (2026-06-23)** — автоподгрузка целей из Метрики. Кнопка «🎯 Подтянуть из Метрики» в карточке
   клиента: счётчик ищется по настройкам кампаний (`yandex.get_campaign_counters` → `TextCampaign.CounterIds`,
   ВСЕ статусы, вкл. ARCHIVED) ∪ по домену (`metrika.get_counters` site ↔ имя клиента), берутся только
   доступные токену, из них тянутся все цели (`metrika.get_counter_goals`) → показываются в карточке →
   правишь и «Сохранить». У клиента бывает много счётчиков, часть 403 — недоступные пропускаются.
   Модуль `metrika.py`, метод `api.metrika_goals`.
   **Доп. (2026-06-23): двухуровневая модель целей.** У цели появилось поле `active` («показывать в отчётах»).
   Тянутся ВСЕ цели; ключевые (тип `e_purchase/form/phone/messenger/action/contact_data_sent` или слова
   покупка/заявка/звонок/форма/заказ…) → `active=true` сразу, остальные сохраняются `active=false` (видно в
   карточке галкой «в отчёт», довключаются). В отчётах (недельных и конструкторе) участвуют только активные
   (`report.goal_defs_from_client(only_active=True)`; backward-compat: цель без `active` = активна). В
   **конструкторе** — блок «Цели для отчёта»: чекбоксы целей клиента (активные предотмечены), выбор шлётся
   как `goal_ids`; пусто → общие конверсии. Массовая кнопка **«🎯 Цели из Метрики (всем)»** в «Клиентах»
   (`api.metrika_goals_bulk`) — подтягивает+сохраняет всем привязанным, сохраняя ручные галки `active`.
   `normalize_goals` хранит `active`+`type`. Дальше можно: «активировать ключевые у всех одной кнопкой».
3. ✅ **СДЕЛАНО (2026-06-23)** — ошибки `run_weekly` пишутся в `send_log` (chat_id=None, status=error) →
   видны в «Истории».
4. **Дубль «рассады цветов» — расследовано 2026-06-23, бага в одиночном прогоне НЕТ.** `run_weekly` идёт по
   уникальным логинам (`set`), `send_for_login` шлёт в каждый чат один раз (`chat_id` — PK). В `send_log` есть
   дубли `(login,chat,неделя)` ×2–4 — это **многократные запуски рассылки за одну неделю** (каждый клик шлёт
   заново). «Рассада 2 раза» в один прогон скорее всего = длинный отчёт >4096 символов → `split_text` шлёт 2
   сообщения Telegram (1 строка в send_log). Живой путь рассылки НЕ трогали. Безопасные опции (согласовать):
   идемпотентность (skip уже `sent` за неделю) + пометка «(часть 1/2)».
5. Опционально: прокси для Telegram (без full-tunnel VPN); привязка нескольких логинов к одному чату.

## Ключевые файлы
- `app/iigbot/report_custom.py` — движок конструктора (LEVELS, ATTRIBUTION_MODELS, `build()`, `to_text()`).
- `app/iigbot/report.py` — недельный движок + `fetch_report(report_type=...)`.
- `app/iigbot/api.py` — backend `Api` (report_options/report_query/report_send, delete_chat, save_secrets, sync_clients, preview, run_weekly, history, settings…).
- `app/iigbot/ui.html` — интерфейс (одна страница; функции renderBuilder/renderChats/renderReports/…). Светлая/тёмная тема.
- `app/iigbot/listener.py` + `bot.py` — слушатель отдельным процессом + одиночный лок (127.0.0.1:49517).
- `app/iigbot/storage.py` — SQLite (chats, clients, bindings, send_log, kv).
- `app/iigbot/settings.py` — пути/секреты/CA-bundle/шаблон secrets.json.
- `.github/workflows/build-exe.yml` — сборка exe → релиз `latest`.

## Как собирать/тестировать
- Правка кода → `git push` в `main` → GitHub Actions собирает exe (~1–2 мин) → скачать из релиза `latest`.
- exe в песочнице запустить нельзя; валидировать так: `python -m py_compile`, импорт модулей, и **живые вызовы
  Reports API** с токеном из `secrets.json` (как в этой сессии) ДО пуша.
- Перед правками **всегда `git fetch`/`pull`** — над репозиторием параллельно работает и другая сессия.

## Полезные разрезы/типы Reports API (проверено)
- Типы отчётов: ACCOUNT/CAMPAIGN/ADGROUP/AD/CRITERIA(фразы)/SEARCH_QUERY _PERFORMANCE_REPORT — все работают.
- Поля-метрики: Impressions, Clicks, Cost, Ctr, AvgCpc, Conversions (работает без Goals!).
- Измерения: CampaignName, AdGroupName, AdId, Criterion, Query, Device, Gender, Age, LocationOfPresenceName, AdNetworkType.
- SEARCH_QUERY и гео дают ОЧЕНЬ много строк (десятки тысяч) — нужен топ-N/пагинация.
